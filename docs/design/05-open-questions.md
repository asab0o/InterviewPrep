# 要確認事項（要件定義 v1.0 に記載がなく、実装前に決定が必要な点）

**2026-07-04時点で Q1〜Q16 すべて解決済み。** 新たな未決事項が出た場合はこの表に追記し、判断を仰ぐこと。
各項目の決定内容は下表参照（Q2〜Q7・Q9〜Q15は2026-07-04に暫定案どおり採用で確定）。

| # | 論点 | 背景（要件の該当箇所） | 暫定案 |
|---|---|---|---|
| ~~Q1~~ | ~~CORS/クッキー設定~~ | **解決済み（2026-07-04決定）**：ドメインは購入せず、Amplify Hostingのリライト機能で`/api/<*>``/auth/<*>`をLightsailへプロキシする方式を採用。ブラウザは常に同一オリジン扱いになるためCORS設定・`SameSite=None`が不要になり、`SameSite=Lax; Secure`で運用する。詳細は`docs/infra-setup.md`0章・7.2章、`docs/design/02-api-design.md`参照 | （解決済みのため暫定案なし） |
| ~~Q2~~ | ~~「総アタック数」にマスタ外を含めるか~~ | **解決済み（2026-07-04決定・暫定案どおり）**：総アタック数は**マスタ紐付きAttemptのみ**を集計する（網羅率と母集団を揃える）。マスタ外Attemptの別枠表示は必要になったら追加 | （解決済みのため暫定案なし） |
| ~~Q3~~ | ~~Anthropic モデルIDの確定~~ | **解決済み（2026-07-04決定・暫定案どおり）**：具体的モデルIDはコードに埋め込まず、環境変数 `ANTHROPIC_MODEL_UMPIRE` / `ANTHROPIC_MODEL_TRANSLATE` で外出しする。初期値は決定時点の最新安定版として UMPIRE解説=`claude-sonnet-5`（Sonnet系）、フレーズ翻訳=`claude-haiku-4-5-20251001`（Haiku系）。※`.env.example`への2変数の追記が未反映（PLAN.md参照） | （解決済みのため暫定案なし） |
| ~~Q4~~ | ~~`umpire_generated_at` 列の追加可否~~ | **解決済み（2026-07-04決定・暫定案どおり）**：追加する（NULL許容のため既存要件に影響なし）。キャッシュ再利用・明示的再生成の運用・監査に使う | （解決済みのため暫定案なし） |
| ~~Q5~~ | ~~`attempts.github_path` 列の追加可否~~ | **解決済み（2026-07-04決定・暫定案どおり）**：追加する。5.4の上書き防止・「アプリ作成ファイルはアプリからのみ更新」をpush済みパスの記録で確実にする | （解決済みのため暫定案なし） |
| ~~Q6~~ | ~~Attempt / Phrase の削除機能~~ | **解決済み（2026-07-04決定・暫定案どおり）**：削除機能を提供する（ミス投入の訂正用）。DELETEエンドポイント＋`phrases`のcascade削除を採用。UIから隠す運用への変更は可 | （解決済みのため暫定案なし） |
| ~~Q7~~ | ~~既存(Obsidian)ファイルへのpush扱い~~ | **解決済み（2026-07-04決定・暫定案どおり）**：許可しない。`createdByApp=false`のファイルへのpushは`force:true`でも拒否する（安全側に倒す） | （解決済みのため暫定案なし） |
| ~~Q8~~ | ~~GitHub push 用トークンの取得元~~ | **解決済み（2026-07-04決定）**：Fine-grained PAT（対象リポジトリ`leetcode-interview-prep`のみ、`Contents: Read and write`権限のみ）を発行し、`.env`の`GITHUB_PUSH_TOKEN`として保持する。ログイン用のGitHub OAuth（認証専用）とは別トークンとし、OAuth側に`repo`スコープは付与しない（スコープを認証専用に保てる／DBへのトークン永続化が不要／対象リポジトリを1つに限定できる、という理由でOAuthトークン再利用より単純かつ安全）。有効期限は最大1年（GitHub側で延長は再発行が必要。期限前にGitHubから通知メールが届く） | （解決済みのため暫定案なし） |
| ~~Q9~~ | ~~`/api/quiz/today` の副作用設計~~ | **解決済み（2026-07-04決定・暫定案どおり）**：GETで抽選＆QuizLog INSERTを行う（UNIQUE制約で複数タブ同時アクセスの二重INSERTを防止）。単一ユーザー・低トラフィックのため`POST /api/quiz/draw`への分離はしない | （解決済みのため暫定案なし） |
| ~~Q10~~ | ~~インポートの冪等キー~~ | **解決済み（2026-07-04決定・暫定案どおり）**：`(categoryId, number, attemptNumber)` を論理キーとして重複スキップ。マスタ外Attemptは`customTitle`で代替判定 | （解決済みのため暫定案なし） |
| ~~Q11~~ | ~~フロント状態管理ライブラリ~~ | **解決済み（2026-07-04決定・暫定案どおり）**：TanStack Query（サーバー状態）＋ React Hook Form + Zod（フォーム）を採用 | （解決済みのため暫定案なし） |
| ~~Q12~~ | ~~型定義の共有方法~~ | **解決済み（2026-07-04決定・暫定案どおり）**：まずはフロント/バックエンド各ディレクトリにDTO型をミラーする。将来必要になったらpnpm workspace機能で`packages/shared`化を検討（パッケージマネージャ自体はpnpmに決定済み→`CLAUDE.md`参照。frontend/backendはそれぞれ独立した`pnpm-lock.yaml`を持つ単純な同居構成とし、現時点ではworkspace化はしない） | （解決済みのため暫定案なし） |
| ~~Q13~~ | ~~リポジトリ配置（front/back同居 or 分割）~~ | **解決済み（2026-07-04決定・暫定案どおり）**：単一リポジトリのmonorepo（`frontend/` `backend/`）。Amplifyはサブディレクトリビルド指定。両ディレクトリはそれぞれ独立したpnpmプロジェクト（各自`pnpm-lock.yaml`を持つ）とし、pnpm workspace機能は現時点では使わない（Q12参照） | （解決済みのため暫定案なし） |
| ~~Q14~~ | ~~フラッシュカードの英語音声読み上げ~~ | **解決済み（2026-07-04決定・暫定案どおり）**：MVPではスコープ外（フェーズ2候補）。5.6の日→英の裏返し表示のみ実装する | （解決済みのため暫定案なし） |
| ~~Q15~~ | ~~記録フォームでの問題文タイトル自動反映の保存~~ | **解決済み（2026-07-04決定・暫定案どおり）**：タイトル行を合成した後のテキストをそのまま`problem_statement`に保存する（UMPIRE入力の再現性重視） | （解決済みのため暫定案なし） |
| ~~Q16~~ | ~~GitHubへpushするMarkdownの本文フォーマット~~ | **解決済み（2026-07-04決定）**：既存Obsidianファイル（`problems/linked-list/21. Merge Two Sorted Lists.md`）を参照しつつ、旧テンプレートのU/M/P（Summary/Pattern/Approach）およびフロントマターの`pattern`/`difficulty`/`mastery`は、実際にはほとんど記入されておらず、かつ現行DBスキーマにも存在しないフィールドだったため廃止。フロントマター自体も「GitHub側だけのメタデータ」を持つと将来のスキーマ変更に追従させる二重管理コストが発生するため廃止し、日付・挑戦回数は見出し行にプレーンテキストで含める方式に統一。UMPIRE解説（`umpire_explanation`）はProblemマスタ側で1問1件管理・アプリ内参照が主目的のためpush本文には含めない。代わりに、旧「Could Not Say」チェックリストの役割を果たす自由記述欄として`attempts.retrospective`（できなかったこと／つまずいた点）を新設。最終テンプレートは`docs/requirements.md` 5.4参照 | （解決済みのため暫定案なし） |

## 特に早期確定が望ましいもの
- ~~Q1（CORS/クッキー/ドメイン）：解決済み（プロキシ方式採用、上表参照）~~
- ~~Q3（モデルID）：解決済み（環境変数で外出し＋決定時点の最新安定版を初期値に、上表参照）~~
- ~~Q8（pushトークン）：解決済み（Fine-grained PAT採用、上表参照）~~
- ~~Q16（Markdown本文フォーマット）：解決済み（フロントマター廃止＋retrospective新設、上表参照）~~
