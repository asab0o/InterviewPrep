{
  "name": "interview-prep-backend",
  "version": "0.1.0",
  "private": true,
  "description": "海外就活準備アプリ バックエンド（Express + TypeScript）。現時点ではヘルスチェックのみのscaffold。",
  "type": "commonjs",
  "engines": {
    "node": "24.x"
  },
  "packageManager": "pnpm@10.14.0",
  "scripts": {
    "dev": "tsx watch src/index.ts",
    "build": "tsc -p tsconfig.json",
    "start": "node dist/index.js",
    "typecheck": "tsc --noEmit",
    "test": "echo \"(scaffold段階のためテスト未実装)\" && exit 0"
  },
  "dependencies": {
    "dotenv": "^16.4.5",
    "express": "^4.21.2"
  },
  "devDependencies": {
    "@types/express": "^4.17.21",
    "@types/node": "^24.0.0",
    "tsx": "^4.19.2",
    "typescript": "^5.7.2"
  }
}
